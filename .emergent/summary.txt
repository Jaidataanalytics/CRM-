<analysis>**original_problem_statement:**
The user's primary goal is to perform a complete data overhaul of their lead management system and introduce new business logic and features. The key requirements are:
1.  **Data Replacement**: Clear the entire database and re-upload ~4 years of data from three newly provided Excel files (, , ).
2.  **Source of Truth for Won Leads**: The  file must be the single source of truth for all Won leads. The dashboard KPIs for  and  must align perfectly with this file.
3.  **Data Discrepancy Reporting**:
    *   Implement a new tab in the Data Management section called Won Without SO to list all leads that are marked as  or  in the  but do not have a corresponding entry in the .
4.  **New Duplicate Detection Logic**:
    *   Implement a new chunk-based merging logic: For each phone number, group all its associated leads, sort them by date, and merge all open leads into the next  lead in the sequence.
    *   The  (e.g., enquiry number, date, stage) should be stored in the primary lead.
    *   Answer the user's question about why the old logic didn't merge all duplicates.
5.  **New Feature: Order Time Punch**: Add a new tab in Data Management to report on leads that were closed (won) within 2 days of their creation date, with summaries for same-day, 1-day, and 2-day closures.
6.  **UI and Filter Enhancements**:
    *   Remove the Qty metric from the Total Leads KPI card on the main dashboard.
    *   Ensure all tabs within the Data Management page respect the global date filters applied on the dashboard.
7.  **Deployment Failure**: The user has provided logs for a failed deployment and needs the agent to analyze the logs, identify the root cause, and fix the application code to ensure a successful deployment.

**User's preferred language**: English

**what currently exists?**
The agent has successfully re-uploaded all the new data and implemented the requested features. The  is now the source of truth for won leads, and the dashboard KPIs reflect this. The Data Management page has been significantly enhanced with new tabs for Order Time Punch and Won Without SO to track quick wins and data discrepancies, respectively. The new, complex chunk-based duplicate detection logic has been implemented and run as a migration. However, the application is currently in a **non-deployable state**. A long-running data migration on application startup is causing the backend health check to fail during deployment.

**Last working item**:
*   **Last item agent was working**: Diagnosing a critical deployment failure. The user provided deployment logs indicating that the backend is failing its health check due to a  error from MongoDB during a data migration that runs at startup.
*   **Status**: BLOCKED
*   **Agent Testing Done**: Y (for features), N (for deployment fix)
*   **Which testing method agent to use?**: backend testing agent. The primary task is to fix the code causing the deployment failure. This involves analyzing  and  to move the long-running migration out of the synchronous startup path. After the fix, the agent should confirm the application runs locally and then use the  to verify a successful deployment.
*   **User Testing Done**: N

**All Pending/In progress Issue list**:
*   **Issue 1: Deployment Failure: Backend Health Check Timeout (P0 - BLOCKER)**
    *   **Description**: The application fails to deploy because a long-running duplicate detection migration is triggered on application startup. This process takes too long, preventing the FastAPI server from becoming ready. As a result, the Kubernetes health check probe fails after multiple attempts, and the deployment is rolled back. The error log explicitly shows a  error during this migration.
    *   **Attempted fixes**: None. The agent was paused immediately after being given the logs.
    *   **Next debug checklist**:
        1.  **Analyze Startup Script**: Examine  to identify where the  migration is being called during the startup event.
        2.  **Optimize or Relocate the Migration**: The migration processing 30,000+ leads is too heavy for a startup task. The fix is to move it out of the blocking startup path.
            *   **Recommended Fix**: Modify  to run the migration as a FastAPI . This will allow the server to start immediately and respond to health checks while the migration runs in the background.
            *   **Alternative**: Create a new, dedicated API endpoint that must be called manually to trigger the migration post-deployment.
        3.  **Verify Fix**: After applying the code change, confirm the backend starts up quickly without errors. Then, trigger a new deployment.
    *   **Why fix this issue and what will be achieved with the fix?**: This is a critical blocker. Fixing it will make the application deployable and functional in the production environment.
    *   **Status**: IN PROGRESS
    *   **Is recurring issue?**: N
    *   **Should Test frontend/backend/both after fix?**: Backend. The success of the fix is measured by a successful deployment.
    *   **Blocked on other issue**: No, this is the highest priority blocker.

**In progress Task List**:
*   None. All development work is blocked by the deployment failure.

**Upcoming and Future Tasks**
*   **Upcoming Tasks**:
    *   **Funnel Analysis (P1)**: Create a new visualization to track conversion rates at each stage (Enquiry → Quotation → Won).
    *   **Manual 'Qualified' Toggle (P1)**: Add a UI element on the lead detail view to allow users to manually set a lead's  status.
    *   **Lead Velocity & ROI Analysis (P2)**: Suggest and potentially implement new analytics for how fast leads move through stages.
*   **Future Tasks**:
    *   Dashboard customization.
    *   Export to Excel functionality for all pages.
    *   Verify Per-Dimension Forecast Accuracy.
    *   Detailed Audit Logs.

**Completed work in this session**
- **Full Data Re-upload**: Successfully wiped the database and populated it with ~4 years of data from the user's new Excel files.
- **New Duplicate Detection Logic**: Implemented and executed a chunk-based duplicate merging algorithm as requested by the user.
- **KPI and Data Integrity Fix**: Made the  the single source of truth for Won leads, correcting the dashboard KPIs to match the raw sales data.
- **New Feature - Order Time Punch**: Built a new report in the Data Management section to identify leads closed within two days of creation.
- **New Feature - Won Without SO**: Created a discrepancy report to show leads marked as Won in the enquiry system but missing from the official sales records.
- **UI/UX Improvements**:
    - Removed the Qty count from the Total Leads dashboard card.
    - Added global date filter functionality to the Data Management page.

**Code Architecture**
has_so_record

**Key Technical Concepts**
*   **Application Startup anti-patterns**: Long-running, blocking tasks (like data migrations) during application startup must be avoided as they interfere with health checks in containerized environments.
*   **Background Tasks**: The appropriate pattern for handling long-running operations in a web server context without blocking startup or user requests. FastAPI's  is the ideal solution here.
*   **Data Reconciliation**: The process of comparing two datasets (Enquiry vs. Sales) to identify discrepancies and establish a single source of truth.

**key DB schema**
*   **leads**:
    *    - New field indicating if a lead is confirmed by the Sales Order register. This is the new source of truth for Won status.
    *    - New field storing an array of enquiry objects that were merged into this lead by the new duplicate logic.

**All files of reference**
*    (The entry point where the failing startup migration is called)
*    (Contains the  function that is timing out)
*    (Frontend for the new data management features)
*   Deployment logs provided by the user.

**Critical Info for New Agent**
1.  **Deployment is Failing**: Your number one priority is to fix the deployment failure. The application is not usable in production until this is resolved.
2.  **The Root Cause is Known**: The deployment logs clearly indicate the issue is a MongoDB read timeout during a duplicate detection migration that runs on application startup. This is a blocking task preventing the server from becoming healthy.
3.  **The Fix**: You must modify  to prevent the  function from blocking startup. The recommended approach is to wrap it in a FastAPI . This allows the server to start immediately while the migration runs in the background. Do not alter the business logic itself, only how it is executed.
4.  **Do Not Rerun Migrations Needlessly**: The data migrations have already been run successfully in the development environment. The goal is to make them non-blocking for production deployment, not to change the data again.

**documents and test reports created in this job**
*   
*   

**Last 10 User Messages and any pending HUMAN messages**
1.  **User**: Provided deployment logs showing the app is failing and asked for a fix.
2.  **User**: Confirmed  should be the source of truth for Won leads.
3.  **User**: Asked to see Won leads without a corresponding SO record in a separate tab.
4.  **User**: Questioned why dashboard  were higher than the  data.
5.  **User**: Explained the new chunk-based duplicate logic (merge open leads into subsequent closed leads).
6.  **User**: Asked for an explanation of why the old duplicate logic didn't merge all leads.
7.  **User**: Requested the Order Time Punch feature and other UI/filter changes.
8.  **User**: Provided a corrected  file and requested a full data re-upload.
9.  **User**: Reported that the dashboard data was wrong after the first data upload attempt.
10. **User**: Provided the initial set of new data files and asked for a database replacement.

**Project Health Check:**
*   **Broken**: The application is non-deployable due to a backend health check failure caused by a long-running startup migration.

**3rd Party Integrations**
*   **rapidfuzz**: Python library for string matching.

**Testing status**
*   **Testing agent used after significant changes**: YES
*   **Known regressions**: The entire application is non-deployable.

**Credentials to test flow:**
*   **Admin**:  / 

**What agent forgot to execute**
The agent implemented all requested features but was stopped by the user reporting a deployment failure. The agent has not yet implemented the fix for the deployment issue.</analysis>
