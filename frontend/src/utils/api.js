// Centralized API configuration
// Uses relative URL so it works in both preview and deployed environments
// The /api prefix is routed to the backend by the ingress

const API = '/api';

export default API;
